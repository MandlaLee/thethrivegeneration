(()=>{const form=document.getElementById('thrive-booking-form');if(!form)return;const textarea=form.querySelector('textarea[name="message"]');const count=form.querySelector('[data-count]');const status=document.getElementById('booking-form-status');textarea?.addEventListener('input',()=>count.textContent=String(textarea.value.length));form.addEventListener('submit',event=>{event.preventDefault();status.className='form-status';if(!form.checkValidity()){form.reportValidity();status.textContent='Please complete the required fields before submitting.';status.classList.add('error');return}status.textContent='Placeholder submission received. The Google Sheets and email connection will be added in a later integration bundle.';status.classList.add('success');form.reset();if(count)count.textContent='0'})})();
